const length = Number(prompt('Please enter the length(cm).'));
const width = Number(prompt('Please enter the width(cm)'));
// console.log(length, width, 45);
//const p = length + width + length + width7;
// console.log(p);
const area = width * length;
console.log(`The calculated area is : ${area}cm`);
